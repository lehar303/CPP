#include<iostream>
#include "compare.hpp" //preprocess


int main(){

    int maximun = max(34,56);
    int minimum = min(34,56);
    std::cout << "Max: " << maximun << std::endl;
    std::cout << "Min: " << minimum << std::endl;

    return 0;
}

